<Grid x:Class="CMG.License.UI.Views.OpenLogFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
      xmlns:prism="http://prismlibrary.com/"
      prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel LastChildFill="True">
        <GroupBox DockPanel.Dock="Top" Header="CMG Log File" Height="100" Margin="10">
            <StackPanel Orientation="Horizontal">
                <Button 
                    Content="Browse" 
                    HorizontalAlignment="Left" 
                    Margin="10,0,0,0" 
                    Height="20" 
                    Command="{Binding GetLogFilePathCmd}"/>
                <TextBlock Text="{Binding LogFilePath}" />
            </StackPanel>
        </GroupBox>
        <ProgressBar DockPanel.Dock="Top" />
        <Border HorizontalAlignment="Stretch"  BorderBrush="#D5DFE5" BorderThickness="1" Margin="10" />
    </DockPanel>
</Grid>